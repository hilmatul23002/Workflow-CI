name: mlflow-autolog-ci

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      dataset:
        type: string
        default: "datasekolahsiap.csv"
    command: "python modelling.py {dataset}"
